<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Alcaldia" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_black</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BottomNavigation">
        <item name="android:background">@color/light_black</item>
        <item name="itemIconTint">@color/primary_red</item>
        <item name="itemTextColor">@color/white</item>
    </style>
</resources>